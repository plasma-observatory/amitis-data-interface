# Simple interface for using AMITIS datasets

.. to look at e.g. pressure-strain interaction terms at jets.

Run `pressure_strain.py` first to generate pressure strain terms intermediate file and then `data_3D_PiD.py` for an example handling of interpolating data to the constellation trajectory.